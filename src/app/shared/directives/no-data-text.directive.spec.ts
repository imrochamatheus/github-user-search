import { NoDataTextDirective } from './no-data-text.directive';

describe('NoDataOpacityDirective', () => {
  it('should create an instance', () => {
    // const directive = new NoDataTextDirective();
    // expect(directive).toBeTruthy();
  });
});
